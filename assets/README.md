# Assets

Diagram source is maintained in `docs/PAPER_TREE.md` as Mermaid. No renderer-only dependency is installed in release 0.1; generated SVG/PNG exports may be added later without replacing the Mermaid source of truth.
